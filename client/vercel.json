{
  "buildCommand": "npm ci --legacy-peer-deps && npm run build",
  "outputDirectory": "build",
  "nodejs": "18.x",
  "env": {
    "CI": "false"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
